query GetAllWorkouts {
  workouts {
    id
    title
    workoutDays {
      day
      exercices {
        id
        name
        repetitions
        series
        muscle
      }
    }
  }
}
