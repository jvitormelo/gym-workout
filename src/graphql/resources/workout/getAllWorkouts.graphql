query GetAllWorkouts {
  workouts {
    id
    title
    workoutDays {
      day
      exercises {
        id
        name
        repetitions
        series
        muscle
      }
    }
  }
}
